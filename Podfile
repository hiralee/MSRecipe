# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MSRecipes' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Contentful', '5.0.5'

  target 'MSRecipesTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
